import 'package:flutter/material.dart';

class AdminOpeningsPage extends StatelessWidget {
  const AdminOpeningsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Openings page not available.')),
    );
  }
}

